# Module `generics-basics.nova`

*Source file: `examples/generics-basics.nova`*

---

## Functions

### `fn identity`
```nova
fn identity(x: T) -> T
```

### `fn pair`
```nova
fn pair(a: A, b: B) -> Int
```

### `fn first`
```nova
fn first(p: TTupleExpr(span=Span(start=684, end=690), elems=[TName(span=Span(start=685, end=686), name='A', args=[]), TName(span=Span(start=688, end=689), name='B', args=[])])) -> A
```

### `fn main`
```nova
fn main(rt: Runtime) -> Int ! {Runtime}
```
