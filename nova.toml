[package]
name = "nova"
version = "1.0.0"
edition = "2026"
description = "NOVA 1.0 Genesis: A Constraint-Native Programming Language & Unified Application Platform"

[dependencies]
# Example:
# analytics = { version = "1.0.0", capabilities = ["Network"] }

[capabilities]
allowed = ["Runtime", "Clock", "Filesystem", "Network"]
