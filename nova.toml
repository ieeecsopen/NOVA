[package]
name = "nova"
version = "0.2.0"
edition = "2026"
description = "NOVA 0.2 — a constraint-native programming language (research preview)"

[dependencies]
# Example:
# analytics = { version = "1.0.0", capabilities = ["Network"] }

[capabilities]
allowed = ["Runtime", "Clock", "Filesystem", "Network"]
